/*
Example file to show how to set up a skeleton test
 */
const path = require('path');
const assert = require('assert');
describe('Example mocha test', () => {
    it('verifies that 1 is indeed 1', function() {
        assert.equal(1, 1);
    });
});
